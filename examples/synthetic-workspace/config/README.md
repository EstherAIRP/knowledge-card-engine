# Synthetic Config

此目錄只保存公開測試用的合成設定，不含密鑰或真實人工設定。

`taxonomy.yaml` 是此 synthetic Workspace 的受控詞彙來源，用來驗證 Knowledge Card 的 source type、resource kind、分類、導覽、relevance dimensions、actions 與 status membership。
