# Synthetic Workspace Fixture

This directory contains synthetic-only test material for the public engine.

It is **not** the Knowledge Card V2 workspace contract. `workspace.yaml`, `engine.lock.json`, profile layout, content layout, and version compatibility are intentionally deferred to T03.
